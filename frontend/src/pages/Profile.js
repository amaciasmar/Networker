import React from 'react';
import ProfilePage from '../components/ProfilePage'; // Assuming ProfilePage is a reusable component

const Profile = () => {
    return (
        <div>
            <ProfilePage />
        </div>
    );
};

export default Profile;